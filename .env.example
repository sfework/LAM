# 本项目不使用配置文件存放业务配置——所有业务配置（模型绑定、注入预算、提取调度、
# CodeGraph 等）都存在 SQLite（data/gateway.db），通过 /api/settings 热更新。
#
# 仅以下三个环境变量在“进程启动层面”被允许读取，其余一律忽略：

# 主服务监听端口（默认 8790；本机 localhost）
PORT=8790

# 数据目录：SQLite 主库、备份、日志都落在此目录（默认 ./data）
DATA_DIR=./data

# 前端构建产物目录（默认 <仓库根>/frontend/dist，与启动目录无关）。
# 存在 index.html 时，主服务在同一端口托管前端 SPA（未命中的 GET 回退 index.html）；
# 不存在则服务以纯后端模式运行。仅当把产物放到别处时才需设置。
# FRONTEND_DIST=./somewhere/dist

# —— MCP（Streamable HTTP，挂主服务 /mcp，与主服务同进程）——
# 检索族（memory_search/conversation_search）滚动限流窗口毫秒（默认 30000，窗口内 ≤3 次）
# MCP_SEARCH_WINDOW_MS=30000
